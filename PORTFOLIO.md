# 待辦清單 Web App

這是一個在 GitHub Copilot 實戰工作坊完成的待辦清單 Web App，使用純前端技術實作，協助使用者新增、整理與追蹤日常待辦事項。作品包含淺色與深色模式、清單篩選，以及已完成事項的批次清理功能。

## 線上展示

[開啟 GitHub Pages](https://<你的帳號>.github.io/<你的repo名稱>/)

> 請將上方網址替換成實際的 GitHub Pages 網址。

## 功能

- 新增待辦事項，空白內容不會被新增。
- 勾選待辦事項為已完成，完成項目會顯示刪除線並淡化。
- 取消已完成狀態，恢復為未完成項目。
- 刪除單筆待辦事項。
- 顯示整體未完成待辦事項數量。
- 顯示清單為空時的提示文字。
- 使用 `localStorage` 保存待辦資料，重新整理後資料仍會保留。
- 切換淺色與深色模式，並在按鈕上顯示目前可切換的模式。
- 保存使用者的主題偏好；使用者尚未手動選擇時，會跟隨作業系統的深淺色設定。
- 依照「全部」、「未完成」與「已完成」篩選待辦事項。
- 篩選結果為空時，顯示對應的提示文字。
- 清除所有已完成事項，刪除前會以瀏覽器內建確認對話框再次確認。
- 沒有已完成事項時，「清除已完成」按鈕會停用。
- 支援手機螢幕與響應式版面。

## 技術

- 使用純 HTML、CSS 與原生 JavaScript。
- 不使用任何框架或套件，也不依賴外部 CDN。
- 使用 CSS 變數集中管理顏色與主題配色。
- 使用 `localStorage` 保存待辦資料與主題偏好。
- 使用 `createElement`、`textContent` 與事件監聽器動態建立及更新待辦清單。
- 可直接以瀏覽器開啟，支援離線使用。

## 開發方式

- 使用 GitHub Copilot Agent Mode，根據功能需求逐步建立待辦清單的 HTML、CSS 與 JavaScript。
- 使用 MCP 查詢 Microsoft Learn 官方文件，了解 `prefers-color-scheme` 與深色模式的色彩對比檢查方式。
- 使用 GitHub MCP 讀取 issue、整理需求，並在完成修正後建立 Pull Request。
- 使用 `.github/prompts/fix-issue.prompt.md` 定義處理 GitHub issue 的 agentic workflow，依序完成讀取 issue、提出計畫、修改、驗證、提交、推送與建立 PR。
- 使用瀏覽器實際驗證新增、完成、篩選、主題切換、清除已完成事項與資料保存等功能。

## 我學到什麼

- 如何使用原生 JavaScript 管理 DOM、事件與 `localStorage`，完成具備多項互動功能的前端應用程式。
- 如何以 CSS 變數設計可切換的淺色與深色主題，並考量色彩對比與使用者偏好。
- 如何將功能需求拆成小範圍修改，透過瀏覽器操作驗證實際行為。
- 如何使用 MCP 連接官方文件與 GitHub 資訊，讓開發流程能取得更貼近情境的參考資料。
- 如何使用 prompt 定義可重複的 issue 修復流程，並以 Pull Request 完成變更交付。
